Web UI fonts (@font-face in static/css/iraniu.css)
--------------------------------------------------
  inter/     Inter variable weights (woff2) — Latin UI
  persian/   Persian.ttf (or Vazir) — RTL fallback family "VazirFallback"

Add Persian.ttf under persian/ if you use the Persian @font-face rule.
